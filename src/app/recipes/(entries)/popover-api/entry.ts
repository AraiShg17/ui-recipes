import type { RecipeEntry } from "@/app/recipes/types";

const entry: RecipeEntry = {
  slug: "popover-api",
  title: "Popover API",
  tags: ["html", "css", "a11y", "chrome150"],
  category: "Components",
  code: "",
};

export default entry;
