import type { RecipeEntry } from "@/app/recipes/types";

const entry: RecipeEntry = {
  slug: "background-clip-border-area",
  title: "background-clip: border-area",
  tags: ["css", "chrome150"],
  category: "CSS Styling",
  code: "",
};

export default entry;
