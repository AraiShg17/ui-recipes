import type { RecipeEntry } from "@/app/recipes/types";

const entry: RecipeEntry = {
  slug: "selectedcontent-custom-select",
  title: "selectedcontent custom select",
  tags: ["html", "css", "chrome150"],
  category: "Forms",
  code: "",
};

export default entry;
