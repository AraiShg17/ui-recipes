import type { RecipeEntry } from "@/app/recipes/types";

const entry: RecipeEntry = {
  slug: "speculation-rules",
  title: "speculation rules (prerender)",
  tags: ["performance", "html", "navigation"],
  category: "Performance",
  code: "",
};

export default entry;
